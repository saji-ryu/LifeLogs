include include/base.pug
include include/head.pug

body
  - for (var i = 0; i < msg.length; i++) {
    - let id = msg[i]._id;
    - delete msg[i]._id;
    - delete msg[i].time;
    - delete msg[i].istaken;
       li #{JSON.stringify(msg[i])}
        | 
        a(href='/show/edit/'+id) edit
        | 
        a(href='/show/delete/'+id) delete 
  - }
  p
    a(href='/') home
